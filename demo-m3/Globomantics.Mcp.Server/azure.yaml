# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: globomantics-mcp-server
metadata:
  template: mcp-server-azd@1.0.1
services:
  mcp:
    project: .
    language: csharp
    host: function
